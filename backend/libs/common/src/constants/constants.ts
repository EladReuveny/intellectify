export enum Microservice {
  USERS_SERVICE = 'USERS_SERVICE',
  AUTH_SERVICE = 'AUTH_SERVICE',
}

export enum TransportPort {
  USERS_SERVICE = 3001,
  AUTH_SERVICE = 3002,
}

export enum Queue {
  USERS_QUEUE = 'users_queue',
  AUTH_QUEUE = 'auth_queue',
}
